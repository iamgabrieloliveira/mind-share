// import { client } from '../../http/client.ts';

// async function persistLike(post: { }, userId: number) {
  // const isLiked = post.isLikedByUser(userId);
  // const url = isLiked ? 'api/like' : 'api/unlike';
  // await client.post(`${url}/${post.getId()}`);
// }

// export { persistLike };
